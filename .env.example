# The submitted demo is deterministic and does not require secrets.
NEXT_PUBLIC_DEMO_MODE=true

# Future server-side implementation only; not read by the static demo.
# OPENAI_API_KEY=
# OPENAI_MODEL=
